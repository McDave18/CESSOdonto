import { SumadientePipe } from './sumadiente.pipe';

describe('SumadientePipe', () => {
  it('create an instance', () => {
    const pipe = new SumadientePipe();
    expect(pipe).toBeTruthy();
  });
});
